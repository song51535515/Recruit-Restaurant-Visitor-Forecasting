# Recruit-Restaurant-Visitor-Forecasting
This project implements an ensemble solution (LightGBM + CatBoost) for the Kaggle competition, leveraging the 1st place feature engineering strategy with log-space ensemble to predict restaurant visitor counts.
